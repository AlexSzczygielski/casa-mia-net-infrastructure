# docker-compose.yml (vaultwarden)
services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vaultwarden.ts.casamia-net.top"
    volumes:
      - /opt/docker-data/vaultwarden:/data/